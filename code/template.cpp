/**
 * Estructura estándar para Competitive Programming
 * Incluye optimizaciones de I/O y definiciones de tipos comunes.
 */
#include <bits/stdc++.h>
using namespace std;

// --- Tipos de datos comunes ---
using ll = long long;
using vi = vector<int>;
using pii = pair<int, int>;

// --- Constantes ---
const int INF = 1e9;
const ll LINF = 1e18;
const int MOD = 1e9 + 7;

// --- Solución del problema ---
void solve() {
    int n;
    cin >> n;
    
    // Tu lógica aquí
    cout << "Input: " << n << "\n";
}

int main() {
    // Optimización para I/O rápido
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t = 1;
    // cin >> t; // Descomentar para problemas con múltiples casos de prueba
    while(t--) {
        solve();
    }

    return 0;
}
// Force cache refresh
